@model IEnumerable<FormularioAsistencia.Models.PDF>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PDF_File)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PDF_File)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_PDF">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_PDF">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_PDF">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
